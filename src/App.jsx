import React from "react";
import { hot } from "react-hot-loader";
import "./App.css";

const App = () => {
  return <div className="main_wrapper">Hello, World !!</div>;
};

export default hot(module)(App);
